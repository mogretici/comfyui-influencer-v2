# Extra model paths for ComfyUI
# Models stored on RunPod Network Volume for fast cold starts

network_volume:
    base_path: /runpod-volume/models
    checkpoints: checkpoints
    unet: unet
    diffusion_models: diffusion_models
    clip: text_encoders
    vae: vae
    loras: loras
    controlnet: controlnet
    upscale_models: upscale_models
    sams: sams
    ultralytics: ultralytics
    ultralytics_bbox: ultralytics/bbox
    ultralytics_segm: ultralytics/segm
    clip_vision: clip_vision

xlabs:
    base_path: /runpod-volume/models/xlabs
    xlabs_ipadapters: ipadapters
    xlabs_controlnets: controlnets

insightface:
    base_path: /runpod-volume/models/insightface
    models: models

pulid:
    base_path: /runpod-volume/models
    pulid: pulid
